{
  "name": "prelude",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.26.0",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "demo:down": "./scripts/run-with-env.sh sh -c 'export POSTGRES_PORT=${POSTGRES_PORT:-5440} REDIS_PORT=${REDIS_PORT:-6380}; docker compose --profile marketing-demo stop candidate realtime interviewer-agent && docker compose --profile marketing-demo rm -f candidate realtime interviewer-agent demo-bootstrap demo-config-check'",
    "demo:logs": "./scripts/run-with-env.sh sh -c 'export POSTGRES_PORT=${POSTGRES_PORT:-5440} REDIS_PORT=${REDIS_PORT:-6380}; docker compose --profile marketing-demo logs -f candidate realtime interviewer-agent demo-bootstrap demo-config-check'",
    "demo:up": "./scripts/run-with-env.sh sh -c 'export POSTGRES_PORT=${POSTGRES_PORT:-5440} REDIS_PORT=${REDIS_PORT:-6380}; docker compose --profile marketing-demo up -d --build --wait'",
    "lint": "turbo run lint",
    "marketing-demo:seed": "node scripts/seed-marketing-demo-roles.mjs",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@prelude/config": "workspace:*",
    "@types/node": "^20.19.5",
    "eslint": "^9.39.2",
    "prettier": "^3.8.4",
    "turbo": "^2.9.18",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
