# dependencies
node_modules
.pnp
.pnp.*

# build outputs
.next
out
dist
coverage
playwright-report
test-results
.turbo
*.tsbuildinfo

# env
.env
.env.*
!.env.example
# dotenvx: the encrypted env files are committed (values are ciphertext);
# the private decryption keys (.env.keys) are NEVER committed.
!/.env
!apps/console/.env.local
.env.keys
**/.env.keys
livekit.toml

# local
.DS_Store
*.log
