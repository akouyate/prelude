# dependencies
node_modules
.pnp
.pnp.*

# build outputs
.next
out
dist
coverage
playwright-report
test-results
.turbo
*.tsbuildinfo

# env
.env
.env.*
!.env.example
# dotenvx: the encrypted console env is committed (values are ciphertext);
# the private decryption keys are NEVER committed.
!apps/console/.env.local
.env.keys
**/.env.keys
livekit.toml

# local
.DS_Store
*.log
