# dependencies
node_modules
.pnp
.pnp.*

# build outputs
.next
out
dist
coverage
playwright-report
test-results
.turbo
*.tsbuildinfo

# env
.env
.env.*
!.env.example
# dotenvx: the single encrypted root .env is committed (values are ciphertext);
# the private decryption key (.env.keys) is NEVER committed.
!/.env
.env.keys
**/.env.keys
livekit.toml

# local
.DS_Store
*.log
